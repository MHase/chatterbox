
<div class="row login-page">
  <div class="left-login col-md-6">
    <h1>Welcome to ChatterBox!</h1>
    <p>ChatterBox is a free-to-use platform that allows users to communicate with their local community!
    Users can create a chat room specific to their location to organize events, meet new people, or just find out what's going on in town.
    Click the button below to sign up for an account and get connected!</p>
    <br>
    <%= link_to "Click Here To Sign Up!", new_registration_path(resource_name), class: "new-buttons" %>
  </div>

  <div class="right-login card form-align col-md-6">
    <h1>Log in</h1>
    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="form-group">
      <%= f.email_field :email, autofocus: true, class: 'form-control', placeholder: 'E-mail Address' %>
    </div>

    <div class="form-group form-back">
      <%= f.password_field :password, autocomplete: "off", class: 'form-control', placeholder: 'Password' %>
      <%= link_to "Forgot your password?", new_password_path(resource_name) %>
    </div>

    <% if devise_mapping.rememberable? -%>
      <div class="form-group">
        <%= f.check_box :remember_me %>
        <%= f.label :remember_me %>
      </div>
    <% end -%>
      <%= f.submit "Log in", class: 'new-buttons' %>
    <% end %>
  </div>
</div>
